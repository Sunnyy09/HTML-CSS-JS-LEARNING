const name = "john";
age = 21; // undecalred variable

// when we create undeclared variable then it added to the window
// object automatically, the age property is also comes from window object,
// to validate this check the output

console.log(delete name);
console.log(delete age);

// delete operator used to delete the specific kty from the object, it
// returns trur if operation is successfull and false in case of unsuccessfull.
